# Purpose

Set up my Mac just the way I like it so I can be productive right away

## Prerequisites

1. Install Homebrew
1. Install Python 3 via Homebrew
1. Install ansible via pip

## Manual configuration not yet automated

1. Set up git
```
git config --global user.name JZ
git config --global user.email jz@example.com
git config --global alias.br branch
```