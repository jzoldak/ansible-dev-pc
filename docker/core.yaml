- name: Docker
  hosts: 127.0.0.1
  connection: local

  tasks:
    - name: Install Docker Desktop on macOS
      homebrew_cask:
        name: docker
        state: present

    - name: Remove quarantine attribute from Docker
      shell: xattr -d com.apple.quarantine /Applications/Docker.app
      ignore_errors: yes