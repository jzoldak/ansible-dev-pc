- name: tmux
  hosts: 127.0.0.1
  connection: local

  tasks:
    - name: Install tmux
      become: true
      apt:
        package: tmux

    - name: Add tmux configuration file
      become: true
      copy:
        src: .tmux.conf
        dest: $HOME
